// === JS Cheatsheet: null vs undefined =======

// --- null
let age = null;   
// It’s just a special value which represents 
// “nothing”, “empty” or “value unknown”.

// --- undefined
let age;
alert(age); 
// shows "undefined".
// If a variable is declared, but not assigned, 
// then its value is undefined.